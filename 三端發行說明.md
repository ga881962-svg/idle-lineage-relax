# 放置天堂－休閒養老：三端共用

三端使用相同的 Supabase 帳號、角色、贊助鑽石、交易所與雲端存檔。請勿在同一角色的兩台裝置同時操作，以最後一次雲端同步的資料為準。

## 網頁版

此資料夾本身就是靜態網頁版。上傳全部檔案到 Cloudflare Pages、Vercel、Netlify 或 GitHub Pages 後，即可讓玩家用網址開啟遊戲。

部署前，請在 Supabase Authentication 的 URL 設定加入正式網站網址，例如：

`https://你的網域`

## 手機版

網頁版已加入 PWA。玩家在 Android Chrome 或 iPhone Safari 開啟正式 HTTPS 網址後，選擇「加入主畫面」即可安裝。手機版不是另一套存檔，登入同帳號就會讀到相同角色。

## Windows 安裝版

安裝依賴後，執行：

`pnpm install`

接著執行：

`pnpm build:windows`

完成的安裝檔會在 `dist-windows` 資料夾。Windows 版內建本機網頁伺服器，但雲端帳號、角色與交易仍直接連到 Supabase。
