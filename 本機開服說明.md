# 本機線上開服

你的電腦會擔任遊戲網站主機；遊戲素材保留在本機，因此不受 Cloudflare Pages 檔案數量限制。角色、雲端存檔與交易資料仍由 Supabase 管理。

## 第一次準備

1. 從 Cloudflare 官方下載 Windows 64 位元的 `cloudflared`。
2. 將下載的 `cloudflared-windows-amd64.exe` 改名為 `cloudflared.exe`。
3. 建立或開啟遊戲資料夾中的 `tools` 資料夾，將檔案放入其中。
4. 最後路徑必須是：`tools/cloudflared.exe`。

官方下載頁：<https://developers.cloudflare.com/cloudflare-one/connections/connect-networks/downloads/>

## 開啟測試服

雙擊 `開啟線上測試服.cmd`。畫面會顯示一條 `https://xxxxx.trycloudflare.com` 網址。把此網址傳給測試玩家或用手機開啟即可。

- 遊戲主機電腦與該視窗必須保持開啟。
- 關閉後網址會失效；下次開啟會產生新網址。
- 不必設定路由器轉發，也不用公開你的 IP。

## 正式公開

測試穩定後，準備一個固定網域並在 Cloudflare 加入它。屆時可建立「命名 Tunnel」，讓網址固定，也可再設定為 Windows 開機自動開服。

## 不使用 Tunnel：直接由電腦公開網站

遊戲伺服器已改為接受區網連線。使用這種方式需要：

1. 以系統管理員執行 `allow-game-web-firewall.ps1`，開放 TCP 8765。
2. 到你的路由器建立「連接埠轉發」：外部 TCP `8765` 轉到這台電腦的區網 IP、內部 TCP `8765`。
3. 用 `http://你的公開IP:8765/` 讓其他人連線。

這是純 HTTP 測試網址；若要手機安裝、HTTPS 與固定網址，仍建議設定網域與反向代理服務。
