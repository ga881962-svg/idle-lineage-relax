# 單機 GM 控制台與修改入口

## 使用 GM 控制台

1. 執行「開始遊戲.cmd」。
2. 建立或載入一個角色。
3. 點右下角 **GM**，或按 **F10**。
4. 修改數值、發放道具後按「套用」或「發放至背包」。每次操作都會自動重算能力值並存檔。

控制台只影響目前載入的單機角色。等級上限目前設定為 99。

「發放道具」可直接輸入道具編號；例如輸入 `479` 就會發放「真．冥皇執行劍」。也仍可輸入完整道具名稱或原本的內部 ID。

## 從哪裡開始改

| 想改的內容 | 檔案 | 開始位置 |
| --- | --- | --- |
| 道具名稱、能力、價格、技能書 | `js/00-data.js` | `const DB` 的 `items` 區塊（約第 299 行） |
| 怪物血量、攻擊、經驗、地圖 | `js/00-data.js` | `DB.mobs`（約第 1931 行）與 `DB.maps`（約第 3018 行） |
| 各怪物掉寶與機率 | `js/01-drops-config.js` | `const MOB_DROPS`（第 1 行）；機率單位是百分比，例如 `1` 表示 1% |
| 掉落結算、經驗、金幣 | `js/05-kill-progression.js` | `function killMob`（約第 319 行） |
| 角色能力計算 | `js/02-stats-recompute.js` | `function recomputeStats`（第 47 行） |
| 戰鬥攻擊公式 | `js/04-combat-attack.js` | 攻擊與傷害函式 |
| 地圖切換規則 | `js/11-world-map.js` | `function changeMap`（約第 1119 行） |
| 存檔／讀檔 | `js/13-shop-save.js` | `saveGame`、`loadGame` |
| GM 功能本身 | `js/33-gm-console.js` | 檔案頂端的 `MAX_LEVEL` 與 `window.gm...` 函式 |

## 建議的修改順序

先複製整個遊戲資料夾作為備份，再從「道具／怪物資料」與「掉寶機率」開始；這三處是純資料調整，最不容易影響遊戲流程。每次修改後請關閉遊戲、重新啟動，再建立測試角色驗證。

如要新增 GM 按鈕功能，請集中寫在 `js/33-gm-console.js`，不要直接改核心戰鬥或存檔函式。這樣日後更新遊戲內容時，GM 工具較容易保留。
